type selectableParametersConfiguration = {
    workTime:   number,
    relaxTime:  number,
    laps:       number
}

type oneLoopValues = {
    balance: number,
    stateTime: number,
    nextState: string,
  };